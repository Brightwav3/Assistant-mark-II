{
  "name": "mark-ii-meta",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "M.A.R.K. II meta-repository. Holds the ecosystem's cross-boundary documentation and the structure test that keeps it honest.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
